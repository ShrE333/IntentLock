# IntentLock V8 Frontend

Create `apps/web/.env.local` with:

NEXT_PUBLIC_INTENTLOCK_API_URL=https://intentlock-worker.shdixit10.workers.dev

Run from repo root:

```powershell
npm install
npm run dev:web
```

Open http://localhost:3000
